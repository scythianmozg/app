def filter_by_year(data, year=140):
    res = []
    for row in data:
        if row[2] > year:
            res.append(row)
    return res # возвращаем отфильтрованную таблицу — накопленный список списков



def filter_by_year(data, year = 1990):
    res[]
    for row in data:
        if row[2] > year:
            res.append(row)
    return res